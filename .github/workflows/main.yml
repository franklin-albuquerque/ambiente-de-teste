name: Gerar animação

on:
  # Atualizar a cada 6 horas
  schedule:
    - cron: "* */6 * * *"
  workflow_dispatch:

  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Gerar animação do gráfico de contribuições
        uses: Platane/snk/svg-only@main
        with:
          github_user_name: franklin-albuquerque
          outputs: |
            dist/contribuicoes-github-animacao-cobrinha.svg
            dist/contribuicoes-github-animacao-cobrinha-dark.svg?palette=github-dark

      - name: Enviar contribuicoes-github-animacao-cobrinha.svg para o branch output
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
